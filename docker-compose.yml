version: '3.4'
services:
  frontend:
    stdin_open: true
    image: frontend
    build:
      context: .
      dockerfile: ./src/frontend/Dockerfile
  backend:
    image: backend
    build:
      context: .
      dockerfile: ./src/backend/Dockerfile
    ports:
      - 80
      - 443
